public boolean isCacheExpired(){
        Token token = getCachedToken();
        if(token== null ||token.isThresholdLimitReached()){
            return true;
        }
        return false;
    }
how to achieve in case of concurrent thread
